apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tracecat.fullname" . }}-agent-executor
  labels:
    {{- include "tracecat.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent-executor
spec:
  {{- if not (and .Values.keda.enabled .Values.autoscaling.agentExecutor.enabled) }}
  replicas: {{ .Values.agentExecutor.replicas }}
  {{- end }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  selector:
    matchLabels:
      {{- include "tracecat.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: agent-executor
  template:
    metadata:
      labels:
        {{- include "tracecat.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: agent-executor
      {{- if or .Values.reloader.enabled (and .Values.tracecat.temporal.metrics.enabled .Values.tracecat.temporal.metrics.scrape) .Values.podAnnotations }}
      annotations:
        {{- if .Values.reloader.enabled }}
        reloader.stakater.com/auto: "true"
        {{- end }}
        {{- if and .Values.tracecat.temporal.metrics.enabled .Values.tracecat.temporal.metrics.scrape }}
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.tracecat.temporal.metrics.port | quote }}
        prometheus.io/path: {{ .Values.tracecat.temporal.metrics.path | default "/metrics" | quote }}
        {{- end }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    spec:
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      serviceAccountName: {{ include "tracecat.agentExecutorServiceAccountName" . }}
      {{- include "tracecat.podScheduling" (dict "root" . "component" "agent-executor") | nindent 6 }}
      initContainers:
        {{- include "tracecat.initWaitPostgres" . | nindent 8 }}
      containers:
        - name: agent-executor
          image: "{{ include "tracecat.backendImage" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["python", "-m", "tracecat.agent.executor_worker"]
          {{- if .Values.tracecat.sandbox.disableNsjail }}
          {{- include "tracecat.containerSecurityContextExec" . | nindent 10 }}
          {{- else }}
          securityContext:
            privileged: true  # Required for nsjail sandbox mount operations
            capabilities:
              add:
                - SYS_ADMIN  # Required for nsjail sandbox
            seccompProfile:
              type: Unconfined  # Required for nsjail sandbox
          {{- end }}
          {{- if .Values.tracecat.temporal.metrics.enabled }}
          ports:
            - name: metrics
              containerPort: {{ .Values.tracecat.temporal.metrics.port }}
              protocol: TCP
          {{- end }}
          {{- include "tracecat.workerLivenessProbe" . | nindent 10 }}
          env:
            {{- include "tracecat.env.agentExecutor" . | nindent 12 }}
            {{- include "tracecat.env.secrets" . | nindent 12 }}
          {{- include "tracecat.extraEnvFrom" . | nindent 10 }}
          volumeMounts:
            - name: sandbox-cache
              mountPath: /var/lib/tracecat/sandbox-cache
            - name: tmp
              mountPath: /tmp
            {{- include "tracecat.postgres.caVolumeMount" . | nindent 12 }}
            {{- if not .Values.tracecat.sandbox.disableNsjail }}
            - name: dev-tun
              mountPath: /dev/net/tun
            {{- end }}
          resources:
            {{- toYaml .Values.agentExecutor.resources | nindent 12 }}
      volumes:
        - name: sandbox-cache
          emptyDir: {}
        - name: tmp
          emptyDir: {}
        {{- include "tracecat.postgres.caVolume" . | nindent 8 }}
        {{- if not .Values.tracecat.sandbox.disableNsjail }}
        - name: dev-tun
          hostPath:
            path: /dev/net/tun
            type: CharDevice
        {{- end }}
