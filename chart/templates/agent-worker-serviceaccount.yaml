{{- if .Values.agentWorker.serviceAccount.create }}
{{- $annotations := mergeOverwrite (dict) .Values.serviceAccount.annotations .Values.agentWorker.serviceAccount.annotations }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tracecat.agentWorkerServiceAccountName" . }}
  labels:
    {{- include "tracecat.labels" . | nindent 4 }}
  {{- with $annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
